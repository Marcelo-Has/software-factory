# Synthetic fixture module — not a real profile, used only by factory/bench/tests/profiles.
name: demo-be
dimension: backend
status: complete

app_layout:
  - app/api/

scaffold:
  - "demo scaffold step"

commands:
  lint: "echo lint-be"
  test: "echo test-be"
  build: null
  e2e: null

screenshot:
  method: null
  preview_adapter: null

deploy:
  note: "not applicable"

gate_adaptations:
  generic_gate_note: "not applicable"

behaviors:
  applicable: true
  runner: vitest
  mapping: "app/api/tests/behaviors/<feature>.test.ts"
  mock: "demo mock strategy"
